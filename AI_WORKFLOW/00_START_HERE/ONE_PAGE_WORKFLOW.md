# ONE PAGE WORKFLOW — Codex 작업 시작 매뉴얼

이 한 페이지만 따르면 오늘 작업을 안전하게 시작할 수 있다. 상세 규칙은 각 링크 문서에 있다.

## 게임 정체성 (재해석 금지)

> Project P.A.는 **"낮에는 동물 마을을 만들고, 밤에는 그 마을의 유일한 잡화점을 운영하는 3D 코지 라이프 시뮬레이션"**이다.
> 플레이어는 마을 생활의 소비자가 아니라, 그 마을의 생활을 가능하게 만드는 **잡화점 주인**이다.
> 핵심 차별점: **"내가 판 물건이 마을의 풍경과 주민 생활을 바꾼다."**
> 기술 구조: 자원 수집 → 가공/확보 → 진열 → 가격 설정 → NPC 소비 → 판매 통계 → 상점 성장 → 마을 변화 (역-공급망 경영 루프).

**목표는 프로토타입이 아니라 완성 게임이다.** 졸업 시연은 중간 마일스톤일 뿐이다. 범위 판단은 `../01_IDENTITY/PROJECT_PA_SCOPE.md` 기준.

## 작업 순서

1. **사전 점검** — 현재 경로와 Git 루트가 `C:\Users\sdjsd\Desktop\Unity\Project_PA`인지 확인. `git status` 확인. Unity Editor가 열려 있는지 확인. 최근 크래시 리포트(루트 `PROJECT_PA_CRASH_REPORT_*.md` 최신본) 확인.
2. **컨텍스트 읽기** — `DOCS_INDEX.md`에서 이번 작업 유형에 필요한 최소 문서만 고른다. 스프린트별 세부 라우팅은 `Docs/AgentWorkflow/CONTEXT_INDEX.md`.
3. **한 작업 선택** — `../03_TASKS/`의 태스크 하나만 잡는다. 태스크가 없으면 `../06_HANDOFF/HANDOFF_FOR_CODEX.md`의 우선순위를 따른다.
4. **수정 계획 보고** — 코드를 고치기 전에 수정 예정 파일 목록과 접근 방법을 먼저 보고한다.
5. **최소 변경 구현** — 기존 코드/씬을 먼저 조사하고, 가장 작은 유효 변경만 한다. 규칙: `../02_AGENT_RULES/UNITY_CODING_RULES.md`.
6. **검증** — `../04_VERIFICATION/VERIFICATION_RULES.md`의 완료 조건을 채운다. 검증 못 한 항목은 "확인 못 함"으로 명시.
7. **기록** — `../05_LOGS/CHANGELOG_AI.md` + 루트 `PROJECT_PA_STATUS.md` / `PROJECT_PA_TODO.md` / `PROJECT_PA_SESSION_REPORT.md` / `Docs/07_개발일지.md` 갱신.

## 금지사항 (전체 목록: `../02_AGENT_RULES/`)

- 게임 방향/장르 재해석, 감성 서사·철학 게임·다크 경영·순수 타이쿤으로의 변경.
- 전체 시스템 재작성, 기존 작동 기능 삭제, 대형 리팩터링.
- Unity 씬/프리팹/SaveManager 임의 변경. 메인 씬(`Assets/Scenes/Prototype_FirstDay.unity`) 덮어쓰기.
- 외부 패키지 추가. Project_D(참조 프로토타입) 수정/복사.
- `git push`, 승인 없는 커밋, 파괴적 Git 명령(`reset --hard`, `clean -fd` 등).
- 한 작업에서 여러 기능 동시 구현.

## 검증 기준 (요약)

컴파일 통과 → 플레이 모드/검증기 통과 → 상점 진열·가격·NPC 구매·수익 증가 확인 → 저장/불러오기 확인 → (마을 변화 관련 작업이면) 판매→마을 변화 증거 확인. **테스트하지 않은 것을 테스트했다고 말하지 않는다.**

## 멈추는 조건

다음 중 하나면 즉시 멈추고 `../05_LOGS/BUG_LOG.md`에 기록 후 보고한다:

- 컴파일 에러가 2회 반복 수정에도 남음.
- 같은 검증기가 같은 이유로 2회 실패.
- Unity 크래시 아티팩트 발생 (루트 크래시 리포트 규칙 참고).
- 금지 경로(Assets 외부 자산, Project_D, ProjectSettings 등)를 수정해야만 진행 가능.
- 씬/저장 스키마/밸런스/새 게임플레이 시스템 등 사람 승인이 필요한 변경에 도달.
- Git 상태가 예상과 다름 (남의 변경을 덮어쓸 위험).
